#include <iostream>
int main () {
    long long n, a, m, b,c;
    std::cout << "Введите k: ";
    if (!(std::cin >> n >> m) || n,m < 1) {
        std::cerr << "Ошибка: введите другое число!" ;
        return 1;
    }
    if (m >> n){
        
    }
}