#include <iostream>
#include <array>
int main () {

    long long a, n;
        std::cout << "Введите n: ";
        if (!(std::cin >> n) || n < 1) {
            std::cerr << "Ошибка: введите другое число!" ;
            return 1;
        }

    int mass[a];
    std::cout << mass[a];
    return 0;
}